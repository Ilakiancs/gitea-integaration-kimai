# Gitea Configuration
# ------------------
# URL of your Gitea instance (with protocol, without trailing slash)
GITEA_URL=https://your-gitea-instance.com

# Your Gitea API token with appropriate permissions
GITEA_TOKEN=your_gitea_api_token_here

# The organization or user name containing repositories
GITEA_ORGANIZATION=your-organization-name

# Kimai Configuration
# ------------------
# URL of your Kimai instance (with protocol, without trailing slash)
KIMAI_URL=https://your-kimai-instance.com

# Either use username/password:
KIMAI_USERNAME=your_kimai_username
KIMAI_PASSWORD=your_kimai_password

# Or use API token (preferred):
KIMAI_TOKEN=your_kimai_api_token

# Sync Configuration
# -----------------
# Comma-separated list of repositories to sync
REPOS_TO_SYNC=repo1,repo2,repo3

# Path to SQLite database file
DATABASE_PATH=sync.db

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Run in read-only mode without making changes to Kimai
READ_ONLY_MODE=false

# Include pull requests in synchronization
SYNC_PULL_REQUESTS=false

# Rate Limiting Settings
# ---------------------
# Enable/disable API rate limiting to prevent throttling
RATE_LIMIT_ENABLED=true

# Number of requests allowed in the period
RATE_LIMIT_REQUESTS=10

# Time period in seconds for rate limiting
RATE_LIMIT_PERIOD=60

# Pagination Settings
# ------------------
# Number of items to fetch per page
PAGE_SIZE=100

# Maximum number of pages to fetch per endpoint
MAX_PAGES=10

# Caching Settings
# ---------------
# Enable/disable data caching to reduce API calls
CACHE_ENABLED=true

# Cache lifetime in seconds (default: 1 hour)
CACHE_TTL=3600

# Directory to store cache files
CACHE_DIR=.cache

# Export Settings
# --------------
# Enable/disable CSV export of sync data
EXPORT_ENABLED=false

# Directory to store exported CSV files
EXPORT_DIR=exports

# Project Mapping
# --------------
# You can manually map repositories to specific project IDs in Kimai
# Uncomment and modify the following lines as needed:
# PROJECT_MAPPING_REPO1=1
# PROJECT_MAPPING_REPO2=5
# PROJECT_MAPPING_REPO3=8
